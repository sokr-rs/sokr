[package]
name = "sokr"
version = "0.1.0"
edition = "2021"
description = "Sovereign Open Kernel Runtime — immutable sovereign core, everything else a plugin."
license = "MIT OR Apache-2.0"
repository = "https://github.com/sokr-rs/sokr"
homepage = "https://github.com/sokr-rs/sokr"
documentation = "https://docs.rs/sokr"
keywords = ["compute", "runtime", "kernel", "gpu", "agnostic"]
categories = ["hardware-support", "no-std"]
readme = "README.md"
exclude = [".github/", "*.md"]

[lib]
name = "sokr"

[dependencies]
